import React from 'react';

function DisplayNotes(userToDo){
    const showToDo = (data) =>  {
        <div>{data.note}</div>
    }

    return(
        <div>

        </div>
    );
}

export default DisplayNotes;